# 日文鍵盤設定（iPhone 11 ＋ Windows）

打不出日文的話，App 的「句子批改」就沒辦法用。這份 5 分鐘搞定。

---

# 一、iPhone 11

## 加入日文鍵盤

**設定 → 一般 → 鍵盤 → 鍵盤 → 加入新的鍵盤… → 日本語**

點下去之後會問你要哪一種，**兩種都可以勾**：

| 種類 | 長什麼樣 | 適合誰 |
|---|---|---|
| **ローマ字**（羅馬字） | 跟英文鍵盤一樣的 QWERTY | **你先用這個。** 打 `ka` 出現 か、`kyo` 出現 きょ，跟你打注音的邏輯接近 |
| **かな**（假名／12 鍵） | 12 宮格，滑動選音 | 日本人主流。手指習慣之後比較快，但要重新學 |

**建議一開始只勾 ローマ字**，等你 50 音熟了再回來加 かな。

## 怎麼切換

打字時按鍵盤**左下角的地球圖示 🌐**，在中文／英文／日文之間輪流切換。長按地球可以直接跳到指定鍵盤。

## 打字要點

- 打 `wa ta shi` → 出現 わたし → 空白鍵（或候選列）選 **私**
- **促音「っ」**：子音打兩次，`kitte` → きって（切手）
- **撥音「ん」**：打 `nn`，例如 `honn` → ほん。只打一個 `n` 有時會被吃掉
- **長音「ー」**：ローマ字鍵盤上打 `-`（減號）
- **變成片假名**：打完之後在候選列往右滑找片假名，或直接切到片假名候選
- **句號逗號**是「。」「、」，鍵盤上就有

## 找不到「加入新的鍵盤」

確認你是在 **設定 → 一般 → 鍵盤 → 鍵盤**（要進去兩層「鍵盤」），不是第一層那個。

---

# 二、Windows

## 安裝日文輸入法

**設定 → 時間與語言 → 語言與地區 → 新增語言 → 搜尋「日本語 / Japanese」→ 下一步 → 安裝**

語言套件不用全裝，**只要勾「基本輸入」就夠了**，不用裝顯示語言（不然整個系統介面會變日文）。

## 怎麼切換

- **Win + 空白鍵** — 在已安裝的輸入法之間切換
- 切到日文之後，工作列右下角會出現 **「A」或「あ」**
- 按 **`半形/全形` 鍵**（Tab 上面那顆，鍵盤上可能印 `~` 和 `` ` ``）在「A（英數）」和「あ（平假名）」之間切
  - 沒有那顆鍵的話，按 **Alt + `~`**，或直接用滑鼠點工作列上的 A／あ

## 打字要點

跟 iPhone 的 ローマ字 一樣：

- `watashi` → わたし → **空白鍵**轉漢字 → **Enter** 確定
- 促音 `kitte` → きって　·　撥音 `honn` → ほん　·　長音打 `-`
- **F7** 把剛打的字直接轉成片假名（超好用）
- **F6** 轉回平假名　·　**F9** 轉成全形英數
- 選錯字按 **空白鍵**繼續換下一個候選

## 想要更好用的

微軟內建的 IME 夠用了。想要更聰明的預測和更完整的詞庫，可以裝 **Google 日本語入力**（免費），切換方式一樣。

---

# 三、練習用不到鍵盤，只有批改要

提醒一下：コトバ 的**七種練習模式全部都是點選作答，不需要日文鍵盤**，通勤時單手就能練。

鍵盤只有兩個地方會用到：

1. **AI → ✍️ 句子批改** — 你自己寫句子（這是最重要的一塊）
2. **AI → 情境對話／自由聊天** — 想用日文回話的時候

所以鍵盤沒設定好也不影響你每天的 39 題，但**會少掉「產出」那一環**，而那一環才是把「看得懂」變成「寫得出來」的關鍵。
