# コトバ Kotoba — 功能說明

一個只屬於你的日文練習 App。完全免費、離線可用、手機與電腦進度同步。
設計前提是：**你母語是中文，看得懂漢字，但不會讀音、不會助詞、不會動詞變化。**
所以練習重心跟一般日文 App 不一樣。

---

## 一、六種練習模式

| 模式 | 建議每日 | 你看到什麼 | 練什麼 |
|---|---|---|---|
| 🃏 **單字** | 10 題 | 中文→選日文，或日文→選中文（隨機） | 詞彙量。先把量堆起來 |
| ㊙️ **漢字** | 8 題 | 漢字詞，選正確讀音 | **你最容易靠中文猜的地方。** 看得懂 ≠ 唸得出來 |
| ⚡ **動詞** | 8 題 | 動詞原形 ＋ 指定變化，選正確答案 | 變化反射。前八週投報率最高的一塊 |
| 🔗 **助詞** | 6 題 | 句子挖掉一個助詞 | は・が・を・に・で。台灣人失分重災區 |
| 🧩 **組句** | 5 題 | 中文句子 ＋ 打散的詞塊 | 語序與句型結構，等於中翻日 |
| 🎧 **聽力** | 5 題 | **什麼都不給，只播日文** | 耳朵。漢字在這科完全幫不上忙 |

**全部都是點選作答，不用日文鍵盤**，所以通勤時單手就能練。

首頁上方六格是各自的每日進度，**點任何一格就直接進入那個模式**。目標數字可在「我的 → 每日目標」調，設 0 就不列入。

按「▶ 開始練習」會依照還沒達標的項目自動混合出題，一輪最多 40 題。
練習中隨時可以按左上角「← 上一題」回去看前面的題目。

---

## 二、為什麼是這六個

你的漢字背景是加速器，但它同時製造三個盲點，這個 App 就是照著這三個盲點設計的：

**1. 看得懂但唸不出來**
「勉強」你一眼就知道意思，但唸不出 `べんきょう`。JLPT 的讀音題、聽解題全部靠這個。所以「漢字」模式獨立成一個模式，而且選項會刻意給長度接近的假名，不能靠字數猜。

**2. 動詞變化沒有中文可以對應**
中文動詞不變形，所以這對你是全新的東西，而且是後面所有文法的地基。App 內建 **127 個動詞 × 13 種變化 = 1651 種題目**，全部是程式即時算出來的，不是預先寫死的表。
干擾選項用的是**同一個動詞的其他變化**，所以你必須真的分辨「て形」和「た形」，不能靠語感排除。

**3. 聽力是你會掉下去的地方**
N1 每一科都有 19 分的門檻，聽解不過就直接不合格，不管閱讀考幾分。所以聽力模式**只播聲音，不給文字**，逼你用耳朵。

---

## 三、間隔複習（不用自己管）

每答一題，App 會依對錯決定下次什麼時候再考你：

- 答對 → 1 天 → 3 天 → 8 天 → 20 天 → 45 天 → 100 天
- 答錯 → 打回原點，明天再來

每次出題會**優先塞給你該複習的項目**，其次是沒看過的新項目。你只要每天把六項目標填滿就好。

首頁會顯示「今天該複習 N 項」，那是系統排的，做完那些記得最牢。

---

## 四、錯題本

**不用自己管，答錯的會自動進來。**

- 答錯 → 自動收進錯題本，首頁跳出紅色卡片
- 同一題**連續兩次答對** → 自動移出
- 按「看清單」可以看全部錯題，也可以手動移除某一題

---

## 五、題庫（可以自己長大）

內建內容：

| 類別 | 數量 | 說明 |
|---|---|---|
| 單字 | 428 字 | N5／N4，標了詞性與級別 |
| 動詞 | 127 個 | 標了第一／二／三類，含 行く・ある・来る 等例外 |
| 自他動詞 | 24 組 | 開く⇄開ける 這種成對的，N3 之後大量出現 |
| 文法 | 108 條 | N5 51 條 ＋ N4 57 條，每條有意思、接續、例句、中譯 |
| 句子 | 242 句 | 26 個情境，每句有全假名與中譯 |

「題庫」那頁可以搜尋（中文、日文、假名都能搜），**點任何一筆就會唸出來**。

自己加東西有三種方式：

- **➕ 加單字 / 加句子** — 手動輸入
- **✨ 用 AI 產生題庫** — 輸入主題（例如「在日本看醫生」），AI 生 8 句存進你的資料
- 直接編輯 `bank-*.js` 檔案 — 大批新增用，但要重新上傳

前兩種走雲端同步，另一台自動就有，**不用重傳檔案**。

---

## 六、AI 導師（選用，需要金鑰）

支援 Google Gemini（免費）和 Anthropic Claude（付費），在「我的 → AI 設定」一鍵切換，兩把金鑰各自保存。

**建議先用 Gemini**，免費、不用信用卡，一天練幾十題的用量綽綽有餘。

金鑰能解鎖五件事：

1. **✨ AI 拆解** — 答完任何一題都會出現這顆按鈕。AI 會告訴你這句怎麼拆、台灣人最容易在這裡錯什麼、再給一個延伸例句
2. **🎭 情境對話** — 餐廳、便利商店、問路、飯店 check in 等，AI 扮演對方跟你一來一往，**每一輪當場糾正你上一句**
3. **💬 自由聊天** — 想聊什麼都行，聊天中不打斷
4. **📋 檢討報告** — 聊完按一下，給你總評＋程度估計、文法錯誤清單、反覆出現的壞習慣、建議練的 5 個句子。報告會留存最近 20 份，可以比較自己的進步
5. **❓ 文法問答** — 「は 和 が 到底差在哪」這種問題直接問

> **Claude Pro／Max 訂閱不能拿來用**，訂閱和 API 是兩套獨立計費。想用 Claude 要另外儲值。

---

## 七、發音

「我的 → 發音設定」可以調：

- **語速** 0.5～1.2 倍。剛開始建議 0.8，聽習慣了再調回 1.0
- **日文語音** 手機裡裝了幾個就有幾個可選

聽力模式的題目卡上有「🐢 放慢」，會用比設定值再慢 0.3 的速度重播一次，不影響你的設定。

沒有日文語音的話：**iOS 設定 → 輔助使用 → 朗讀內容 → 聲音 → 日文**，下載一個。

---

## 八、手機電腦同步

在手機練完，電腦一開就看得到。**兩邊各自練的都會保留，不是誰覆蓋誰**——同一個項目取「練得比較新」的那筆，自訂題目兩邊合併。

AI 金鑰不會上傳，只留在各自裝置上。

設定步驟見「開始使用.md」第四階段。什麼時候會同步：

- 開啟 App 時拉一次
- 一輪練習結束時上傳
- 切到背景／關掉 App 時上傳
- 從背景切回來超過 3 分鐘也會拉一次
- 隨時可按「🔄 立即同步」

> ⚠️ **和 Echo 英語的關係**：可以共用同一個 Supabase 專案，但資料表不同（`kotoba_sync` vs Echo 的表），同步代碼也各自獨立，不會互相影響。

---

## 九、備份（請認真看這節）

就算開了雲端同步，還是建議偶爾留一份備份碼。

App 在每台裝置同時寫進 **localStorage 和 IndexedDB**，其中一個被清掉會自動從另一個救回，開啟時會跳「已從備援儲存救回你的進度」。**但兩個一起被清就真的沒了**：

- 在 Safari 設定裡按「清除瀏覽資料」
- 把主畫面的 App 刪掉重裝
- 換手機、重置手機
- 儲存空間爆滿時系統自行清理

**備份**：「我的 → 備份與資料 → 📋 複製備份碼」，複製一長串 `KOTOBA1:` 開頭的文字，貼到備忘錄。超過 7 天沒備份，「我的」那頁會跳紅點。

**還原**：新手機裝好 App 後 → 貼上那串碼 → 按還原。學習進度、錯題本、自訂題目、金鑰、所有設定都會完整回來。

也可以用「存成檔案／讀取檔案」走 JSON 檔，適合存到 iCloud 雲碟。

---

## 十、更新程式

改過任何檔案後：

1. 把 `sw.js` 裡的 `kotoba-v1` 往上加一號
2. 整個資料夾重新拖到 **原本那個 Netlify 站台的 Deploys 分頁**
3. 手機打開 App，下方跳出藍色「有新版本可以更新」→ 按一下

App 每次從背景切回前景都會檢查一次，另外每小時也會自己檢查。

---

## 十一、檔案說明

| 檔案 | 用途 |
|---|---|
| `index.html` | App 本體（介面＋所有邏輯） |
| `bank-vocab.js` | 單字庫。想手動加字照格式往下加，一行一個 |
| `bank-verb.js` | 動詞庫 ＋ 自他動詞 ＋ **變化引擎**。加動詞只要填「辭書形\|讀音\|中文\|類別\|級別」，13 種變化會自動算出來 |
| `bank-grammar.js` | 文法庫 |
| `bank-sentence.js` | 句子庫。詞塊之間用 `/` 分開，這個切分同時決定「組句」的積木和「助詞」要挖哪一格 |
| `sw.js` | 離線快取＋自動更新。改過任何檔案後把 `kotoba-v1` 往上加一號 |
| `manifest.webmanifest` | App 名稱與圖示 |
| `icon-192.png` / `icon-512.png` | 主畫面圖示 |

---

## 十二、怎麼配合你的 N1 計畫

這個 App 對應的是你每日四塊裡的**第一塊（單字 SRS）和第二塊（文法主課）的複習部分**，大約 25～35 分鐘。

剩下的還是要靠：

- **影子跟讀 20 分鐘** — App 的聽力模式是「聽懂」，跟讀是「說得出來」，兩件事
- **輸出 25 分鐘** — 自己寫句子，丟給 Claude 批改

階段目標提醒：**2026/12/6 考 N4**（報名 8/17–8/31）。這個 App 的內建題庫剛好涵蓋 N5＋N4 的範圍，考前兩週把錯題本清空就對了。
