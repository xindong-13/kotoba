# コトバ Kotoba — 從零到能用，照著做就好

全部做完約 25 分鐘。**每一步做完再做下一步**，不要跳。
流程跟 Echo 英語一模一樣，你已經熟了。

---

## 你手上有什麼

桌面上的 `コトバ Kotoba` 資料夾，裡面 11 個檔案：

```
index.html              ← App 本體
bank-vocab.js           ← 單字庫 428 字
bank-verb.js            ← 動詞庫 127 個 ＋ 變化引擎 ＋ 自他動詞 24 組
bank-grammar.js         ← 文法庫 108 條
bank-sentence.js        ← 句子庫 242 句
sw.js                   ← 離線與自動更新
manifest.webmanifest    ← App 名稱與圖示設定
icon-192.png            ← 圖示
icon-512.png            ← 圖示
使用說明.md              ← 功能說明（不影響 App，可留可不留）
開始使用.md              ← 你正在看的這份
```

**上傳時 9 個程式檔案全部一起傳，一個都不能少。**（兩個 .md 傳不傳都行。）

---

# 第一階段：把 App 放到網路上（電腦操作，約 5 分鐘）

## 步驟 1｜打開 Netlify Drop

電腦瀏覽器開：**https://app.netlify.com/drop**

## 步驟 2｜把資料夾拖進去

把桌面上整個 **`コトバ Kotoba` 資料夾**，直接用滑鼠拖到那個網頁的虛線框裡放開。

> ⚠️ 是**拖整個資料夾**，不是先打開資料夾再拖裡面的檔案。
> ⚠️ 這會建立一個**全新的站台**，跟你的 Echo 英語是分開的兩個站，不會互相影響。

## 步驟 3｜拿到你的網址

上傳完會出現一個隨機網址，像 `https://random-name-123abc.netlify.app`。
先在電腦上點開它確認能用——應該會看到黑底、上面寫「コトバ Kotoba」的畫面。

## 步驟 4｜註冊帳號把網址固定下來

你上次已經註冊過了，直接登入同一個 Netlify 帳號就好。
登入後改網址名稱：`Site configuration` → `Change site name` → 改成好記的，例如 `jerry-kotoba`。

**不登入認領的話這個網址過幾小時就會失效。**

## 步驟 5｜把網址存起來

記到備忘錄，等一下手機要用。

---

# 第二階段：裝到 iPhone（手機操作，約 2 分鐘）

## 步驟 6｜用 Safari 打開網址

iPhone 上開 **Safari**（一定要 Safari），輸入剛剛那個網址。

## 步驟 7｜加到主畫面

1. 點畫面**最下方中間的「分享」圖示**
2. 往下滑，找到 **「加入主畫面」**
3. 名稱維持 `コトバ` → 右上角 **加入**

主畫面會出現黑底紅色「コ」的圖示。**之後都從這個圖示打開**，會是全螢幕。

## 步驟 8｜設定日文語音（重要）

這個 App 的聽力模式要用到日文語音。第一次先確認手機有裝：

**iOS 設定 → 輔助使用 → 朗讀內容 → 聲音 → 日文** → 下載一個（Kyoko 或 Otoya 都可以）

裝好後回 App →「我的 → 發音設定 → 🔊 試聽」，聽得到日文就對了。

> 沒有日文語音的話，其他五個模式照樣能用，只有「聽力」會沒聲音。

**做到這裡，App 就已經完全可以用了。**下面兩個階段是選配。

---

# 第三階段：開啟 AI 功能（選配，免費，約 3 分鐘）

不做也能用：六種練習模式、間隔複習、錯題本、900 多個練習項目全部正常。
做了才有：AI 拆解句子、文法問答、情境對話、檢討報告、AI 生題。

## 步驟 9｜拿 Google 免費金鑰

開 **https://aistudio.google.com/apikey** → Google 帳號登入 → **Create API key** → 複製整串。

**你 Echo 英語用的那把金鑰可以直接拿來用**，不用重新申請。

## 步驟 10｜填進 App

App →「**我的**」→ **AI 設定**：

1. 上面確認選的是 **Google Gemini（免費）**
2. 金鑰欄位貼上 → 鍵盤按 **完成**
3. 按 **測試連線** → 出現「連線成功 ✅」

> 失敗的話按 **🔍 偵測可用模型**，它會自動挑一個能用的。

---

# 第四階段：手機電腦同步（選配，約 10 分鐘）

只有手機在用的話**可以跳過**。

> ⚠️ **不要跟 Echo 英語共用同一個資料表。** コトバ 用的是 `kotoba_sync` 這張表，Echo 用的是另一張，兩個 App 的 SQL 不一樣，各跑各的。
> 但**可以共用同一個 Supabase 專案**（同一個 Project URL 和金鑰），只是要多跑一段 SQL。

## 步驟 11｜Supabase 專案

已經有 Echo 在用的專案的話，**直接用同一個**，跳到步驟 12。
沒有的話：**https://supabase.com** → New project → 地區選 **Northeast Asia (Tokyo)**。

## 步驟 12｜建資料表

App →「我的 → 雲端同步 → 第一次設定要跑的 SQL」→ 按 **📋 複製這段 SQL**。

回到 Supabase → 左側 **SQL Editor** → **New query** → 貼上 → 按 **Run**，看到 **Success** 就對了。

## 步驟 13｜填進 App

專案畫面最上方的 **Connect** 按鈕，或左側 **Settings → API Keys**，拿兩樣：

| 欄位 | 長什麼樣 |
|---|---|
| **Project URL** | `https://abcdefgh.supabase.co` |
| **金鑰** | `sb_publishable_...`（或舊版 `eyJ...`） |

> ⚠️ **不要**用 `Secret` 或 `service_role` 那把。

回 App「我的 → 雲端同步」貼上，**同步代碼**按 **產生**，再按 **儲存並測試連線**。

> ⚠️ コトバ 的同步代碼要跟 Echo 的**不一樣**（各自按「產生」就會不一樣，不用特別處理）。

## 步驟 14｜設定第二台裝置

在已設定好的那台按 **📋 複製整組設定給另一台**（會複製 `KOTOBASYNC1:` 開頭的字串），
用 LINE 傳給自己 → 另一台打開同一個網址 →「我的 → 雲端同步 → 📥 貼上另一台的設定」。

第二台要**自己再填一次 AI 金鑰**，金鑰基於安全不會上傳。

---

# 題庫要加東西，要不要重傳？

| 加東西的方式 | 要重傳嗎 | 說明 |
|---|---|---|
| App →「題庫」→ **✨ 用 AI 產生題庫** | **不用** | 存在你的資料裡，走雲端同步 |
| App →「題庫」→ **加單字／加句子** | **不用** | 同上 |
| 直接編輯 `bank-*.js` 檔案 | **要** | 那是程式碼，得跟 App 一起重新上傳 |

日常加東西用 App 內建功能就好。想大批新增才走下面的重傳流程。

---

# 之後想改東西的話

**1. 改版本號**

打開 `sw.js`，把第 3 行的 `kotoba-v1` 改成 `kotoba-v2`（下次 v3，一直往上加）。
**這步不做，手機不會知道有新版。**

**2. 重新上傳到「原本那個站台」**

**https://app.netlify.com** → 登入 → 點你的 **kotoba** 站台（不是 echo 那個）→ 上方 **Deploys** → 頁面下方虛線區 → 把整個 `コトバ Kotoba` 資料夾拖進去。

> ⚠️ **不要再用 app.netlify.com/drop**，那會開一個全新站台給你新網址。

**3. 手機更新**

打開 App，下方會跳出藍色「有新版本可以更新」→ 按一下。

---

# 開始練之後，記得做這件事

「我的 → 備份與資料 → **📋 複製備份碼**」，貼到手機備忘錄。

每週一次，10 秒的事。萬一手機資料被清掉，這串碼是唯一能救回全部進度的東西。
超過 7 天沒備份，App 會在「我的」那頁跳紅點提醒你。

---

# 卡住的時候

| 狀況 | 怎麼辦 |
|---|---|
| 網頁一片空白 | 檔案沒傳齊，9 個程式檔要一起傳 |
| 找不到「加入主畫面」 | 不是用 Safari 開的 |
| 聽力模式沒聲音 | 沒裝日文語音，見步驟 8；另外 iPhone 側邊靜音開關要打開，且要先點過畫面一次 |
| 唸出來像中文 | 選到中文語音了，去「我的 → 發音設定 → 日文語音」換一個 |
| AI 說金鑰無效 | Gemini 要在 aistudio.google.com 建的才行 |
| 同步失敗 | 看紅字訊息，多半是 SQL 沒跑或貼到 Secret 金鑰 |
| 手機沒跳更新提示 | `sw.js` 版本號沒改，或還沒重新上傳 |
| 改壞了想重來 | 你桌面資料夾裡的原始檔沒被動過，重新拖一次就好 |
