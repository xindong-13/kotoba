# コトバ Kotoba — 手機同步教學

這份講兩件不同的事，別搞混：

| | 同步什麼 | 什麼時候做 |
|---|---|---|
| **A. 程式同步** | 把電腦上改好的 App 推到手機 | 我改了題庫或功能之後 |
| **B. 進度同步** | 手機練的進度讓電腦看得到，反之亦然 | 設定一次，之後全自動 |

---

# A. 程式同步（電腦 → 手機）

## A-1　第一次（只做一次，約 8 分鐘）

在 `コトバ Kotoba` 資料夾裡，**按兩下 `手機版-第一次設定.bat`**。

它會依序做這些事，你只要跟著畫面走：

1. 檢查 Node.js。沒裝的話會幫你開 nodejs.org，裝完**要重開機**再跑一次
2. 安裝 Netlify CLI（你之前為了 Daily Quest 裝過的話，這步幾秒就結束）
3. 開瀏覽器登入 Netlify（已登入的話會直接跳過）
4. 問你幾個問題，**這裡要小心**：

> **Q: What would you like to do?**
> 選 **`[+] Create & configure a new site`**
>
> ⚠️ **不要選 Link to an existing site，也絕對不要挑到 Echo 或 Daily Quest 的站台。**
> 挑錯的話你會把那個 App 蓋掉。コトバ 必須是自己獨立的站。
>
> **Q: Team** → 直接按 Enter
> **Q: Site name** → 打一個小寫短名字，例如 `jerry-kotoba`

5. 跑完後畫面上會出現 `Website URL: https://xxxxx.netlify.app`
   **把它複製起來，貼到資料夾裡的 `site-url.txt`**（忘記存的話，之後按兩下 `顯示我的網址.bat` 也查得到）

## A-2　裝到 iPhone（約 2 分鐘）

1. iPhone 用 **Safari**（一定要 Safari）打開那個網址
2. 點下方**分享**圖示 → 往下滑 → **加入主畫面** → 右上角**加入**
3. 主畫面出現黑底紅色「コ」的圖示，之後都從這裡打開

**接著設定日文語音**（聽力模式要用）：
iOS 設定 → 輔助使用 → 朗讀內容 → 聲音 → 日文 → 下載一個（Kyoko 或 Otoya）
回 App →「我的 → 發音設定 → 🔊 試聽」，聽得到日文就成功了。

## A-3　之後每次更新（10 秒）

**按兩下 `手機版-一鍵更新.bat`**，就這樣。

它會自動做三件事：

1. **把 `sw.js` 的版本號 +1** — 這步以前你要手動改，忘了改手機就不會知道有新版。現在自動了
2. 把要上傳的檔案整理進 `.deploy` 資料夾（`.md`、`.bat`、`.ps1` 不會上傳）
3. 推到你原本那個站台，網址不變

跑完後打開手機 App，下方會跳出藍色「有新版本可以更新」，點一下就好。
沒跳出來的話把 App 滑掉重開（它每次回前景都會檢查一次）。

## A-4　其他兩個 .bat

| 檔案 | 用途 |
|---|---|
| `顯示我的網址.bat` | 忘記網址時用，會印出來並自動開啟 |
| `列出我的所有站台.bat` | 列出你 Netlify 上所有站台，確認哪個是 コトバ、哪個是 Echo |

---

# B. 進度同步（手機 ⇄ 電腦）

手機練的、電腦練的會合併成同一份。**兩邊各自練的都保留，不是誰蓋誰。**

> ⚠️ AI 金鑰**不會**上傳，只留在各自裝置。第二台要自己再填一次。

## B-1　建資料表

你已經有 Supabase 專案（Daily Quest 在用的那個）的話，**直接用同一個專案就好**，只要多跑一段 SQL。

1. App →「我的 → ☁️ 雲端同步 → 第一次設定要跑的 SQL」→ 按 **📋 複製這段 SQL**
2. 開 https://supabase.com → 進你的專案 → 左側 **SQL Editor** → **New query**
3. 貼上 → 按 **Run** → 看到 **Success** 就好

> コトバ 用的資料表叫 `kotoba_sync`，跟你其他 App 的表是分開的，不會互相影響。

沒有 Supabase 專案的話：Start your project → New project → 地區選 **Northeast Asia (Tokyo)** → 等 2 分鐘。

## B-2　填進 App（第一台）

專案畫面最上方的 **Connect** 按鈕，或左側 **Settings → API Keys**，拿兩樣：

| 欄位 | 長什麼樣 |
|---|---|
| Project URL | `https://abcdefgh.supabase.co` |
| 金鑰 | `sb_publishable_...`（舊版是 `eyJ...`） |

> ⚠️ **不要用 Secret / service_role 那把**，那是給伺服器用的。

回 App「我的 → 雲端同步」：貼上 URL、貼上金鑰、**同步代碼按「產生」**，最後按 **儲存並測試連線**。
看到「雲端同步已啟用 ✅」就完成了。

## B-3　第二台（30 秒）

在**已設定好的那台**按 **📋 複製整組設定給另一台**，會複製一串 `KOTOBASYNC1:` 開頭的文字。
用 LINE 傳給自己 → 第二台打開同一個網址 →「我的 → 雲端同步 → 📥 貼上另一台的設定」→ 套用。

這樣做兩邊的同步代碼一定一樣，不會打錯。

## B-4　什麼時候會同步

- 開啟 App 時拉一次
- 一輪練習結束時上傳
- 切到背景／關掉 App 時上傳
- 從背景切回來超過 3 分鐘再拉一次
- 隨時可按 **🔄 立即同步**

---

# C. 備份（跟同步是兩回事，還是要做）

雲端同步壞掉、或 Supabase 出問題時，備份碼是最後一道防線。

「我的 → 💾 備份與資料 → **📋 複製備份碼**」→ 貼到手機備忘錄。
**每週一次就好**，超過 7 天沒備份 App 會在「我的」那頁跳紅點提醒。

換手機時：新手機裝好 App → 貼上那串 `KOTOBA1:` 開頭的碼 → 按還原。進度、錯題本、課表進度、自訂題目、金鑰全部回來。

---

# ⚠️ Netlify 免費額度（2026 改成點數制，一定要看）

Netlify 在 2026 年把免費方案改成**點數制**，跟以前的「頻寬 100GB」不一樣了：

| 項目 | 扣點 |
|---|---|
| **正式部署（`--prod`）** | **每次 15 點** |
| 草稿部署（draft／preview） | **0 點** |
| 頻寬 | 每 GB 20 點 |
| 網頁請求 | 每 1 萬次 2 點 |
| 失敗的部署、rollback | 不扣點 |

免費方案是 **每月 300 點**，而且**點數是整個帳號共用**——コトバ、Echo 英語、Daily Quest 三個站台一起分這 300 點。

**300 ÷ 15 = 一個月大約 20 次正式部署。**

> ⚠️ **點數用完的後果很嚴重：帳號下的「所有」站台都會被暫停**，訪客看到 `Site not available`，要等下個帳單週期才恢復。也就是說你如果把點數用在 コトバ 上，Echo 也會一起掛掉。免費方案不能加購點數。
> Netlify 會在用到 50%、75%、100% 時寄信提醒你。

## 所以要怎麼用

**1. 先用草稿部署檢查，再花點數正式上傳**

按兩下 **`測試上傳（不扣點數）.bat`**，它會給你一個臨時網址，**0 點**。在那個網址上確認改對了，再跑 `手機版-一鍵更新.bat`（15 點）。
你原本的 App 網址完全不受影響。

**2. 把修改攢起來一次上傳**

一天改五個小地方就上傳五次 = 75 點 = 四分之一的月額度。攢到一起上傳一次 = 15 點。

**3. 抓一個預算**

三個站台加起來，一個月抓 **18 次正式部署**（270 點），留 30 點給頻寬和請求。
你這個 App 全部檔案約 250 KB，而且 PWA 會快取，實際頻寬用量幾乎是 0，所以瓶頸就是部署次數。

**4. 隨時可以查**

按兩下 **`查看我的點數用量.bat`**，會開啟 Netlify 後台。在 **Team settings → Billing** 看得到點數使用進度條。

## 萬一真的不夠用

- **GitHub Pages** 是完全免費的靜態託管，沒有部署次數限制，很適合當備援。要的話跟我說，我幫你寫一支對應的 bat。
- Netlify 下一階 Personal 方案是每月 1,000 點。

---

# 卡住的時候

| 狀況 | 怎麼辦 |
|---|---|
| bat 說 Node.js not found | 去 nodejs.org 裝 LTS，**裝完要重開機** |
| bat 說 not linked to a site | 還沒跑過第一次設定，先跑 `手機版-第一次設定.bat` |
| bat 說 logged out | 開命令提示字元打 `netlify login` |
| 不小心蓋掉別的 App | 去那個 App 的資料夾跑它自己的更新 bat 就會蓋回來 |
| 手機沒跳「有新版本」 | 把 App 滑掉重開；還是沒有就確認 `手機版-一鍵更新.bat` 有跑完 |
| 網頁一片空白 | 用 `.bat` 上傳就不會發生（它會確保檔案齊全）。手動拖曳的話是漏檔案 |
| 同步失敗說找不到資料表 | B-1 的 SQL 沒跑成功，回 SQL Editor 重跑 |
| 同步失敗說權限不足 | 金鑰貼成 Secret 了，要用 Publishable / anon 那把 |
| 兩台進度沒合起來 | 確認兩台的同步代碼一字不差，用「複製整組設定」最保險 |
